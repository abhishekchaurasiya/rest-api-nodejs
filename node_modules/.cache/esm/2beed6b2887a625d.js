let Joi,RefreshModel,UserModel,CustomErrorHandler,bcrypt,JwtServices,RFRESH_SECRET_KEY;_887‍.x([["default",()=>_887‍.o]]);_887‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_887‍.w("../../model",[["RefreshModel",["RefreshModel"],function(v){RefreshModel=v}],["UserModel",["UserModel"],function(v){UserModel=v}]]);_887‍.w("../../service/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_887‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_887‍.w("../../service/JwtServices",[["default",["JwtServices"],function(v){JwtServices=v}]]);_887‍.w("../../config",[["RFRESH_SECRET_KEY",["RFRESH_SECRET_KEY"],function(v){RFRESH_SECRET_KEY=v}]]);






const registerController = {
    async register(req, res, next) {

        // Validation 
        const registerSchema = Joi.object({
            name: Joi.string().min(3).max(35).required(),
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required(),
            repeat_password: Joi.ref("password")
        });

        // error handle of body
        const { error } = registerSchema.validate(req.body);
        if (error) {
            return next(error)
        }

        const { name, email, password } = req.body;
        // check if user is in the database already present
        try {
            const exist = await UserModel.exists({ email });
            if (exist) {
                return next(CustomErrorHandler.alreadyExist("This email is already taken."))
            }
        } catch (error) {
            return next(error)
        };

        // Hashing password
        const hashPassword = await bcrypt.hash(password, 10);

        // prepare the model
        const user = new UserModel({
            name,
            email,
            password: hashPassword
        });

        let access_token;
        let refresh_token;
        try {
            const result = await user.save()
            access_token = JwtServices.sign({ _id: result._id, role: result.role });
            refresh_token = JwtServices.sign({ _id: result._id, role: result.role }, '1y', RFRESH_SECRET_KEY);

            //  database white list (means database me store karna)
            await RefreshModel.create({ token: refresh_token })

        } catch (error) {
            return next(error)
        }

        res.json({ access_token, refresh_token, user: user })
    }
}

_887‍.d(registerController);