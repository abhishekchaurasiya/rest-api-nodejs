let express,mongoose,APP_PORT,DB_URL,errorHandler,path,router;_d53‍.w("express",[["default",["express"],function(v){express=v}]]);_d53‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d53‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}],["DB_URL",["DB_URL"],function(v){DB_URL=v}]]);_d53‍.w("./middlewares/errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_d53‍.w("path",[["default",["path"],function(v){path=v}]]);_d53‍.w("./routes/routes",[["default",["router"],function(v){router=v}]]);





const app = express();


// DB connection
mongoose.connect(DB_URL, { useNewUrlParser: true, useUnifiedTopology: true });

const db = mongoose.connection;
db.on('error', _d53‍.g.console.error.bind(_d53‍.g.console, 'Connection Error'));
db.once('open', () => {
    console.log("Database is connected")
});

// Here gets current folder path
global.appRoot = path.resolve(__dirname)
app.use(express.json());
app.use(express.urlencoded({ extended: false }))
app.use("/api", router)
app.use("/uploads", express.static("uploads"))

// This is a middleware hence last me write karte hai 
app.use(errorHandler)

const start = async () => {
    try {
        app.listen(APP_PORT, () => {
            console.log(`Listening on port ${APP_PORT} `);
        })
    } catch (error) {
        _d53‍.g.console.log(error);
    }
};
start();
